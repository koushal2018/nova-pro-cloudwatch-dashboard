{
  "enabled": true,
  "name": "AWS Parameter Type Validation",
  "description": "Automatically validates AWS-specific parameter types (like AWS::EC2::VPC::Id, AWS::EC2::SecurityGroup::Id, etc.) in CloudFormation templates and infrastructure code to ensure proper configuration",
  "version": "1",
  "when": {
    "type": "fileEdited",
    "patterns": [
      "*.yaml",
      "*.yml",
      "*.json",
      "*.template"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "Review this file for AWS-specific parameter types. Check if Parameters section uses AWS-specific types like AWS::EC2::VPC::Id, AWS::EC2::Subnet::Id, AWS::EC2::SecurityGroup::Id, AWS::EC2::KeyPair::KeyName, etc. instead of generic String types. Validate that these parameter types are used correctly and suggest improvements for automatic validation. Highlight any parameters that should use AWS-specific types for better validation."
  }
}